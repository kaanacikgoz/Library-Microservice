spring:
  application:
    name: borrowing-service
  datasource:
    url: jdbc:mysql://localhost:3306/borrowing_db
    username: mysql
    password: database
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8083